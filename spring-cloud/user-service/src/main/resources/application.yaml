server:
  port: 8088

spring:
  application:
    name: user-service

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: "http://localhost:8762/eureka,http://localhost:8761/eureka"
